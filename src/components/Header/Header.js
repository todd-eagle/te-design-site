import React from 'react'
import {MainHeader, Logo} from '../../styles/Layout/Header'

const Header = () => {

    return(
        <MainHeader>
            <Logo>
                me.
            </Logo>
        </MainHeader>
        )
}

export default Header